# Environment Variables (CONTAINS SENSITIVE CREDENTIALS)
.env

# Documentation files (internal use only, not for public GitHub)
CLAUDE.md
SECURITY.md
CHANGES.md
GITHUB_PUBLISH_SUMMARY.md

# Node modules
node_modules/

# Connection data (may contain sensitive customer/entity IDs)
connections/*.json
!connections/.gitkeep

# Plaid connection data (may contain sensitive access tokens and account info)
plaid-connections/*.json
!plaid-connections/.gitkeep

# Webhook event data (may contain sensitive item IDs and account info)
webhooks/*.json
!webhooks/.gitkeep

# OS Files
.DS_Store
Thumbs.db

# IDE Files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
.cache/
